<%= form_for(@postnotice) do |f| %>
  <% if @postnotice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@postnotice.errors.count, "error") %> prohibited this postnotice from being saved:</h2>

      <ul>
      <% @postnotice.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :author %><br />
    <%= f.text_field :author %>
  </div>
    <div class="field">
    <%= f.label :city %><br />
    <%= f.select :city_id, City.all.collect {|c| [c.name, c.id]} %>
  </div>
  <div class="field">
    <%= f.label :categorie %><br />
    <%= f.select :categorie_id, Categorie.all.collect {|c| [c.name, c.id]} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
